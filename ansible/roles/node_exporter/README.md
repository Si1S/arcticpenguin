# Rôle — node_exporter

Installe Prometheus Node Exporter (binaire + service systemd).

## Utilisation

Inclus dans `playbooks/common.yml` et `playbooks/exporters.yml`.

```bash
ansible-playbook playbooks/exporters.yml -K
