<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sauve les Pingouins{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    {% block stylesheets %}{% endblock %}
            {{ encore_entry_link_tags('app') }}
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">🐧 Sauve les Pingouins</a>
            <div class="navbar-nav">
                <a class="nav-link" href="{{ path('app_donation') }}">Faire un don</a>
		<a class="nav-link" href="{{ path('app_blog') }}">blog</a>
                <a class="nav-link" href="{{ path('app_adhesion') }}">Devenir membre</a>
                <a class="nav-link" href="{{ path('app_intranet') }}">Connexion</a>
            </div>
        </div>
    </nav>

    <main class="container-fluid">
        {% block body %}{% endblock %}
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    {% block javascripts %}{% endblock %}
            {{ encore_entry_script_tags('app') }}
</body>
</html>
